def solution(n, computers):
    answer = 0

    for idx, raw in enumerate(computers):
        for idx_raw, raw_ele in enumerate(raw):
            

    return answer

computers = [
    [1,0,1],
    [0,1,0],
    [1,0,1],
]
n = 3

solution(n, computers)